const express = require('express');
const router = express.Router();

// Placeholder route handler
router.get('/', (req, res) => {
  res.send('Placeholder route for add staff');
});

module.exports = router;
